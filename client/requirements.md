## Packages
framer-motion | Complex animations for punishment completion and interactions
recharts | Visualizing curse statistics and trends
clsx | Conditional class merging (standard utility)
tailwind-merge | Conflict-free class merging (standard utility)

## Notes
Replit Auth is used.
- Login: `window.location.href = "/api/login"`
- Logout: `window.location.href = "/api/logout"`
- Protected routes need `useAuth` hook check.
